# PayBack Frontend Environment Configuration

# Base URL for FastAPI Backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Data Mode: 'api' (connects to FastAPI + Supabase backend) or 'mock' (uses in-memory demo datasets)
NEXT_PUBLIC_DATA_MODE=api

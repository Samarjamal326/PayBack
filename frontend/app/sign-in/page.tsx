'use client'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { FormEvent, useState } from 'react'
import { setDemoSession } from '@/lib/demo-session'
export default function SignInPage() { const router = useRouter(); const [loading, setLoading] = useState(false); function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); const data = new FormData(e.currentTarget); setDemoSession({ email: String(data.get('email') || 'admin@payback.io'), name: 'Aditi Sharma' }); router.push('/dashboard') } return <main className="auth-page"><div className="auth-card"><Link href="/" className="auth-logo">PayBack</Link><h1>Welcome back</h1><p>Sign in to your recovery workspace.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required placeholder="you@company.com" /></label><label>Password<input name="password" type="password" required placeholder="••••••••" /></label><div className="auth-options"><label className="check"><input type="checkbox" /> Remember me</label><a href="#forgot">Forgot password?</a></div><button className="button-primary" type="submit" disabled={loading}>{loading ? 'Signing in…' : 'Sign in'}</button></form><button className="button-secondary demo-button" type="button" onClick={() => { setDemoSession({ email: 'merchant@payback.demo', name: 'Demo Merchant' }); router.push('/dashboard') }}>Use demo account</button><p className="demo-hint">merchant@payback.demo · password: demo</p><div className="auth-switch">New to PayBack? <Link href="/sign-up">Create an account</Link></div></div></main> }
